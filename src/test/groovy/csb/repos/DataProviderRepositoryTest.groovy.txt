package csb.repos
import csb.config.AppConfig
import csb.model.DataProviderEntity
import org.junit.runner.RunWith
import org.slf4j.Logger
import org.slf4j.LoggerFactory
import org.springframework.test.context.ContextConfiguration
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner
/**
 * Created by dneufeld on 12/13/15.
 */
@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(classes=AppConfig.class)
class DataProviderRepositoryTest {

    private static final Logger logger =
            LoggerFactory.getLogger( DataProviderRepositoryTest.class )


    //@Test
    public void saveDps( ) {
        repository.save( new DataProviderEntity( "SEAID", "support@seaid.org", "support@seaid.org", "support@seaid.org") )
        repository.save( new DataProviderEntity( "LINBLAD", "support@expeditions.com", "support@expeditions.com", "support@expeditions.com") )

        for (DataProviderEntity dpe : repository.findAll()) {
            logger.debug( dpe.toString() )
        }
    }
}


